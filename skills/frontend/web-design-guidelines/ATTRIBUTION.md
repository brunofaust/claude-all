# Attribution

This skill is a **local wrapper** — its `SKILL.md` is original to claude-all and
**fetches the upstream guidelines live at runtime** (via WebFetch). It does **not**
vendor/redistribute the upstream content, so it always uses the latest and needs no
sync step.

- **References:** https://github.com/vercel-labs/web-interface-guidelines (`command.md`, ref: `main`)
- **Upstream author / license:** Vercel Labs, MIT.

Listed in `vendored.json` as a `reference` entry (no files copied — nothing to update).
