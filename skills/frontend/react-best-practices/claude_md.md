## React / Next.js — `react-best-practices` skill
Apply when writing/editing React/Next.js code (`*.tsx`, `*.jsx`, `*.ts` in `pages/`, `app/`, `src/components/`).

Watch for: inline-object props causing re-renders, unnecessary `useEffect`, server/client component boundary mistakes.
