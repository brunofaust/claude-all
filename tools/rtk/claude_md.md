## RTK — token-killer
Installed and active. Priority: built-in tools (`Read`/`Glob`/`Grep`) → agents → `rtk <cmd>` → raw Bash. Use `rtk discover --all --since 30` to audit missed savings; `rtk gain` for stats; `rtk proxy <cmd>` for unfiltered output.
