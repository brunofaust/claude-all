Delegate quality checks to `code-quality`, fixes to `lint-fixer`. Inspect every `[FILES MODIFIED BY THE GATE]`; undo only unintended hook changes, preserving pre-existing edits. Reject claimed fixes; verify snapshots.
