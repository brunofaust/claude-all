### `frontend-builder` (Haiku) — frontend builds
| `npm/pnpm/yarn run build`, `vite build`, `next build`, `astro build`, `nuxt build`, `tsc -b`, `webpack/rollup/esbuild` | `frontend-builder` |
⛔ `Bash(npm run build)`, `Bash(vite build)`, `Bash(next build)` inline
Note: never starts a dev server (long-running process — keep in tmux/background).
