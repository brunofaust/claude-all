### `email-inspector` (Haiku) — email inspection
| Check/filter/search/summarize email via Gmail/Outlook MCP tools | `email-inspector` |
⛔ Calling email MCP tools (`search_threads`, `read_thread`, `get_thread`) directly in main session
Note: read-only — never sends/archives/deletes/labels.
