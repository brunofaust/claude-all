Validate contracts against real DB/SDK/schema/CI behavior. Encode repeatable rules as checks. Keep root rules global; preserve every rule's reachable owner when relocating.

Contract changes: load `mock-drift-sweep`; new gates: `regression-gates`; retrospectives: `diff-retrospective`. Zero tests or any failure blocks completion. Pin full runtimes; allow nonblocking CI only for proven environmental flakiness with a restoration plan.

One owner per external system; extract before copy three; no speculative/junk-drawer modules or module-private names hiding dead code. Wire settings through code, every deployment and docs; no tunable constants. Bake assets in every build; log fetch fallbacks.

Mark idempotency after success or release in finally; report partial batch failures; exhaust pagination; keep one migration head and test fresh migrations. Redact customer repr; no global tenant cache; pass argv lists. Round-trip real LLM output through schemas/consumers; constrain with enums; port tests with SDKs. Verify parallel changes together; prefer edit-time guards.
