# Attribution

Vendored from a third-party project. See the central registry `vendored.json`
(repo root) for the authoritative source + sync metadata; update with
`python scripts/vendor_sync.py --id vercel-react-view-transitions`.

- **Source:** https://github.com/vercel-labs/agent-skills (path: `skills/react-view-transitions`,
  ref: `4ec6f84b61cd3c931046c3e6e398f3ae7de372f7`)
- **Author:** Vercel
- **License:** MIT — declared inline in `SKILL.md` frontmatter (`license: MIT`,
  `metadata.author: vercel`). Upstream ships **no separate LICENSE file**, so the
  license is recorded here and in the frontmatter rather than as a vendored file.
- **Local changes:** none to content; pinned to the last internally coherent upstream bundle because
  a later revision updated the source/reference files without regenerating its compiled `AGENTS.md`.
