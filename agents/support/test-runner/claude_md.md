### `test-runner` (Haiku) — run tests
| `pytest`, `uv run pytest`, `npm test`, `pnpm test`, `vitest`, `jest`, `go test`, `cargo test` | `test-runner` |
⛔ `Bash(uv run pytest ...)`, `Bash(pytest ...)`, `Bash(npm test)`, `Bash(cd "..." && pytest ...)` inline
✓ OK inline: single targeted `pytest path::test_x -q` while actively debugging.
