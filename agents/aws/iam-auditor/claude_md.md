### `iam-auditor` (Haiku) — IAM inspection
| `aws iam get-role/get-role-policy/get-policy/get-policy-version/simulate-principal-policy` | `iam-auditor` |
⛔ `Bash(aws iam get-role-policy ...)`, `Bash(aws iam get-policy-version ...)`, `Bash(aws iam simulate-principal-policy ...)` inline
✓ OK inline: simple list calls (`list-role-policies`, `list-attached-role-policies`) when only names are needed.
