### `cost-explorer` (Haiku) — AWS Cost Explorer
| `aws ce get-cost-and-usage` etc. — spend totals / trends / forecast / cost-by-tag / anomalies | `cost-explorer` |
⛔ `Bash(aws ce get-cost-and-usage ...)` inline
Note: resource-level waste hunt → `cost-audit-runner`. Cost Explorer API charges $0.01 per request.
