### `s3-inspector` (Haiku) — S3 bucket / object inspection
| `aws s3 ls`, `aws s3api list-objects`, bucket lifecycle / encryption / versioning, "size of this prefix" | `s3-inspector` |
⛔ `Bash(aws s3 ls ...)`, `Bash(aws s3api list-objects ...)` inline
Note: read-only — never uploads / downloads / deletes objects or changes bucket policy.
