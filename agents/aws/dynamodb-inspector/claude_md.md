### `dynamodb-inspector` (Haiku) — DynamoDB reads
| `aws dynamodb get-item/query/scan/describe-table/list-tables` | `dynamodb-inspector` |
⛔ `Bash(aws dynamodb query ...)`, `Bash(aws dynamodb get-item ...)` inline
Note: for WRITES use `dynamodb-mutator` (requires explicit confirmation).
