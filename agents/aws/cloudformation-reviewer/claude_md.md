### `cloudformation-reviewer` (Sonnet) — CFN template/change-set review
| "review this CloudFormation", "audit CFN template", "is this stack safe to deploy", "review the change set" | `cloudformation-reviewer` |
⛔ Reviewing CFN templates inline in main session
Note: read-only; never `create-stack`/`update-stack`/`execute-change-set` — those go through `cloudformation-deployer`.
