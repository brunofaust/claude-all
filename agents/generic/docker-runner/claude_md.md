### `docker-runner` (Haiku) — docker / compose
| `docker build/run/exec/logs/ps/images`, `docker compose up/down/restart/logs` | `docker-runner` |
⛔ `Bash(docker build ...)`, `Bash(docker compose up ...)`, `Bash(docker logs ...)`, `Bash(cd "..." && docker compose ...)` inline
✓ OK inline: trivial 1-shot check (`docker ps -q`, single container health).
