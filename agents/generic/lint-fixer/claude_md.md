### `lint-fixer` (Sonnet) — fix lint/type/quality findings
| "fix lint/ruff/mypy/eslint/tsc/prek errors", "resolve codecongruence", "make prek pass" | `lint-fixer` |
| Report findings without fixing; run linters to SEE findings | `code-quality` (Haiku) |
| Proactive style modernization (PEP 695, async, etc.) | `python-refactorer` |
⛔ `Bash(ruff check ...)`, `Bash(mypy ...)`, `Bash(eslint ...)`, `Bash(prek run ...)` inline
⛔ Silencing findings with `# noqa`/`# type: ignore`/`--no-verify` — fix root cause instead
