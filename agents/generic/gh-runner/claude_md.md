### `gh-runner` (Haiku) — GitHub `gh` CLI (read-only)
| `gh pr list/view/checks`, `gh issue list/view`, `gh repo view`, `gh release list`, `gh run list/view --log` | `gh-runner` |
⛔ `Bash(gh pr view ...)`, `Bash(gh run view --log)`, `Bash(gh pr list)` inline
✓ OK inline: tiny 1-shot `gh` call (`gh pr view --json number -q .number`).
