### `python-deps` (Haiku) — dependency managers
| `uv sync/add/remove/lock/upgrade`, `uv pip install`, `pip install/uninstall`, `poetry add/remove/update/lock`, `pipx install/upgrade` | `python-deps` |
⛔ `Bash(uv sync)`, `Bash(pip install ...)`, `Bash(poetry lock)` inline
Note: for `uv run pytest` use `test-runner`; for `uv run mypy`/`ruff` use `code-quality`/`lint-fixer`.
